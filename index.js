const AbstractLogger = require('./src/abstractlogger');
const Level = require('./src/level');
const LogManager = require('./src/logmanager');

module.exports = {
    AbstractLogger: AbstractLogger,
    Level: Level,
    LogManager: LogManager
};
